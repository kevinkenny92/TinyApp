<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <% include partials/_header.ejs %>
  <ul>
    <% for(var key in urls) { %>
      <li>
        <%= key + ': ' + urls[key] %>
        <a href="/urls/<%= key %>">Edit</a>
        <form method="POST" action="/urls/<%= key %>/delete"><button>Delete</button></form>
      </li>
    <% } %>
      <li>
        <a href="/urls/new">Click here to shorten the URL</a>
      </li>
  </ul>
</body>
</html>